# Weather MCP

MCP server that provides current weather and 7-day forecast for any city via Open-Meteo.

## Setup

```bash
npm install
npm run build
```

## Usage

Configure as an MCP server in Cursor or Claude Desktop. Tools: `get_current_weather`, `get_weather_forecast`.
